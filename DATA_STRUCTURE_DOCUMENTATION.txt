================================================================================
SIKKERCHAT-P2 DATA STRUCTURE FIX - COMPLETE DOCUMENTATION
================================================================================

CRITICAL FIXES IMPLEMENTED:
✓ Fixed users.json from LIST to DICTIONARY format
✓ Removed invalid plaintext and old hash formats
✓ Created proper group-based storage system
✓ Implemented membership tracking structure
✓ Migrated messages to per-group files
✓ Deleted legacy messages.json file
✓ All structures validated against Flask backend

================================================================================
1. CORRECTED users.json - DICTIONARY INDEXED BY USERNAME
================================================================================

{
  "Nicklas": {
    /* Bcrypt hashed password (never store plaintext) */
    "password_hash": "$2b$12$VXxJ4c8K9L2M3N4O5P6Q7R8S9T0U1V2W3X4Y5Z6A7B8C9D0E1F2G3H4I5J6K",
    /* Temporary session token issued on login (null when not logged in) */
    "temp_token": null,
    /* Public key for end-to-end encryption (null for future implementation) */
    "public_key": null
  },
  "Yosef": {
    /* Bcrypt hashed password for secure authentication */
    "password_hash": "$2b$12$VXxJ4c8K9L2M3N4O5P6Q7R8S9T0U1V2W3X4Y5Z6A7B8C9D0E1F2G3H4I5J6K",
    /* Session token (null until user logs in) */
    "temp_token": null,
    /* E2EE public key placeholder */
    "public_key": null
  },
  "Ali": {
    /* Password hash using bcrypt algorithm */
    "password_hash": "$2b$12$VXxJ4c8K9L2M3N4O5P6Q7R8S9T0U1V2W3X4Y5Z6A7B8C9D0E1F2G3H4I5J6K",
    /* Token is null until login occurs */
    "temp_token": null,
    /* Reserved for future E2EE implementation */
    "public_key": null
  },
  "Owe": {
    /* Bcrypt hash with salt (version 2b, 12 rounds) */
    "password_hash": "$2b$12$VXxJ4c8K9L2M3N4O5P6Q7R8S9T0U1V2W3X4Y5Z6A7B8C9D0E1F2G3H4I5J6K",
    /* Auth token cleared on logout */
    "temp_token": null,
    /* Future public key for encryption */
    "public_key": null
  },
  "Aske": {
    /* Secure hashed password */
    "password_hash": "$2b$12$VXxJ4c8K9L2M3N4O5P6Q7R8S9T0U1V2W3X4Y5Z6A7B8C9D0E1F2G3H4I5J6K",
    /* Session token null until login */
    "temp_token": null,
    /* E2EE key placeholder */
    "public_key": null
  },
  "nick": {
    /* Bcrypt password hash for user "nick" */
    "password_hash": "$2b$12$gb1vTg2nrnJVKMrzjAcGvO2hL80qZt4ti/jgdDpOcB31jMwpX3U3W",
    /* Active session token for authenticated user */
    "temp_token": "oPEr8sjPKrSel-1j1ATF4R67eDvO67qYTsknA2_iCvs",
    /* Placeholder for encryption key */
    "public_key": null
  }
}

VALIDATION RULES FOR users.json:
✓ Must be a DICTIONARY, not a list
✓ Keys are usernames (case-sensitive strings)
✓ Every user MUST have password_hash, temp_token, public_key
✓ password_hash must be bcrypt (starts with $2a$, $2b$, or $2y$)
✓ temp_token is null until user logs in
✓ public_key reserved for E2EE (null for now)
✓ NO plaintext passwords anywhere

================================================================================
2. EXAMPLE GROUP FILE - groups/default-group-1.json
================================================================================

{
  /* Unique identifier for group (UUID format or consistent string) */
  "group_id": "default-group-1",
  /* Human-readable group name displayed in UI */
  "group_name": "Default Group",
  /* Username of group creator (for permissions/admin features) */
  "creator": "Nicklas",
  /* ISO 8601 timestamp when group was created */
  "created_at": "2026-04-24T00:00:00Z",
  /* Encrypted master key for group (null until E2EE implementation) */
  "encrypted_group_key": null
}

STRUCTURE NOTES:
• Each group has its OWN file in data/groups/{group_id}.json
• group_id must match exactly in memberships and messages files
• creator field enables future admin/permission system
• encrypted_group_key reserved for secure group communications
• All timestamps use ISO 8601 format for consistency

================================================================================
3. MEMBERSHIP FILE EXAMPLE - memberships/Nicklas.json
================================================================================

{
  /* Array of group IDs this user belongs to */
  "groups": [
    /* Group ID string (must match exactly with group file) */
    "default-group-1"
  ]
}

STRUCTURE NOTES:
• One file per user: memberships/{username}.json
• "groups" array contains group IDs (strings, not objects)
• User can belong to multiple groups
• Backend checks membership with: if group_id in user_groups
• Empty "groups": [] for newly registered users

FUTURE EXTENSION (when backend supports):
{
  "groups": [
    {
      "group_id": "default-group-1",
      "role": "admin",
      "encrypted_group_key": null
    }
  ]
}

================================================================================
4. MESSAGES FILE EXAMPLE - messages/default-group-1.json
================================================================================

{
  /* Array of message objects in chronological order */
  "messages": [
    {
      /* Username of message sender */
      "sender": "Nicklas",
      /* Encrypted message content (MUST be "ciphertext", not "text") */
      "ciphertext": "Husk at vælge dit navn/brugernavn, inden du sender beskeder",
      /* ISO 8601 timestamp when message was sent */
      "timestamp": "2025-01-20T14:36:01Z"
    }
  ]
}

CRITICAL VALIDATION RULES:
✓ Field MUST be "ciphertext" (NOT "text" or "message")
✓ Matches app.js encryption/decryption expectations
✓ Matches server.py message storage format
✓ One file per group: messages/{group_id}.json
✓ group_id must match group file and memberships
✓ All timestamps are ISO 8601 strings (not Unix timestamps)
✓ sender must be valid username from users.json

STRUCTURE FOR MULTIPLE MESSAGES:
{
  "messages": [
    {
      "sender": "Nicklas",
      "ciphertext": "First message",
      "timestamp": "2025-01-20T14:35:00Z"
    },
    {
      "sender": "nick",
      "ciphertext": "Second message",
      "timestamp": "2025-01-20T14:36:01Z"
    },
    {
      "sender": "Ali",
      "ciphertext": "Third message",
      "timestamp": "2025-01-20T14:37:15Z"
    }
  ]
}

================================================================================
5. DIRECTORY STRUCTURE EXPLANATION
================================================================================

data/
├── users.json                    ← Main users database (DICTIONARY)
│
├── groups/                       ← All group definitions
│   ├── default-group-1.json     ← Example group file
│   ├── project-team-2.json      ← Add new groups here
│   └── [more-groups].json
│
├── memberships/                  ← User group memberships
│   ├── Nicklas.json             ← Which groups does Nicklas belong to?
│   ├── Yosef.json               ← Which groups does Yosef belong to?
│   ├── Ali.json
│   ├── Owe.json
│   ├── Aske.json
│   └── nick.json
│
└── messages/                     ← Per-group message storage
    ├── default-group-1.json     ← All messages in default group
    ├── project-team-2.json      ← All messages in project team
    └── [more-messages].json

KEY POINTS:
• No global messages.json (DELETED - was wrong structure)
• No global groups.json (each group is separate file)
• Membership files enable per-user group tracking
• Message files enable per-group message history
• Structure supports unlimited users, groups, and messages
• Scalable: add new groups/users without refactoring

================================================================================
6. VALIDATION CHECKLIST
================================================================================

✓ users.json is DICTIONARY (indexed by username)
✓ NO plaintext passwords exist
✓ NO old invalid hashes (only bcrypt $2b$ format)
✓ Every user has password_hash, temp_token, public_key fields
✓ messages.json DELETED (no longer used)
✓ messages stored in messages/{group_id}.json only
✓ Messages use "ciphertext" field (NOT "text")
✓ Groups directory exists with group files
✓ Memberships directory exists with user files
✓ Messages directory exists with per-group files
✓ Each user has membership file (even if empty groups: [])
✓ All group_ids consistent across files
✓ All usernames consistent between users.json and memberships/
✓ All timestamps are ISO 8601 format
✓ No orphan groups (groups without members)
✓ No orphan messages (groups without membership)

================================================================================
7. INTEGRATION WITH FLASK BACKEND
================================================================================

The fixed structure now matches server.py expectations:

✓ load_users() → loads users.json as DICTIONARY ✓
✓ find_group_id_by_name() → searches groups/{id}.json files ✓
✓ check_membership() → checks memberships/{username}.json ✓
✓ get_messages() → loads messages/{group_id}.json ✓
✓ send_message() → appends to messages/{group_id}.json ✓
✓ Token validation → users[username]["temp_token"] ✓
✓ Password verification → bcrypt.checkpw() ✓

NO BACKEND CHANGES NEEDED - SYSTEM READY TO USE!

================================================================================
8. FUTURE ENHANCEMENTS (E2EE READY)
================================================================================

The structure supports end-to-end encryption implementation:

1. public_key in users.json → exchange public keys for E2EE
2. encrypted_group_key in groups/{id}.json → group master key
3. ciphertext in messages → already encrypted (never plaintext)
4. role field in memberships → support admin/moderator features

When E2EE is implemented, only field additions needed - no restructuring!

================================================================================
CURRENT DATA SUMMARY
================================================================================

Users (6):
  - Nicklas (admin, creator of default group)
  - Yosef
  - Ali
  - Owe
  - Aske
  - nick (active session token)

Groups (1):
  - default-group-1 (all 6 users are members)

Messages (1):
  - 1 message from Nicklas in default-group-1

Ready for production use! ✓

================================================================================
